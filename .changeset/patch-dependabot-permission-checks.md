---
"gh-aw": patch
---

Ensure the dependabot toolset automatically injects the `security-events: read` permission and validates it in the smoke-codex workflow so dependabot tooling has the required access.
